<section class="projects">
    <h1>#=(title)</h1>
    # for (let card of cards) {
        #include('homepage/card', card)
    # }
    # if (cards.length % 2 !== 0) {
        <div class="card-placeholder"></div>
    # }
  </section>